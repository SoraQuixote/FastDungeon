<form method="post" action="{{ path('app_pnj_delete', {'id': pnj.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pnj.id) }}">
    <button class="btn">Delete</button>
</form>
